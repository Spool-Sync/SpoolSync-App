# Database
DATABASE_URL="postgresql://spoolsync:password@localhost:5432/spoolsync?schema=public"

# App
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d


# OpenID Connect (optional)
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_CALLBACK_URL=http://localhost:3000/api/v1/users/auth/oidc/callback

# Open Filament Database (api.openfilamentdatabase.org) is used automatically.
# No additional config needed for filament sync.
